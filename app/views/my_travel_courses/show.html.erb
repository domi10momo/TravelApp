<% @course.each do |spot| %>
  <p>
    <%= spot.order %>. <%= @spots.find(spot.spot_id).name %>
    <% if @schedule.gone %>
      <%= link_to "感想の入力", new_impression_path(spot) %>
    <% end %>
  </p>
<% end %>
<%= button_to "感想入力に進む", my_travel_courses_gone_flag_path(@schedule) %>